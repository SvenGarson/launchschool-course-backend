=begin
  
  pop returns and removes that last object from the array  
  size returns the length of the string, when called on
  a string. So the statement results in:

    11

=end

puts ['ant', 'bear', 'caterpillar'].pop.size